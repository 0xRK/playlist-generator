# Example .env file. Rename the file to .env and update the values.

# Spotify OAuth Configuration
SPOTIFY_CLIENT_ID=client_id
SPOTIFY_CLIENT_SECRET=client_secret
REDIRECT_URI=http://127.0.0.1:3001/auth/callback
CLIENT_URL=http://localhost:5173

# Whoop API Configuration
# Get these from https://developer.whoop.com/
# 1. Sign up for Whoop Developer Platform
# 2. Create a new application
# 3. Copy your Client ID and Client Secret
# 4. Set redirect URI to: http://127.0.0.1:3001/auth/whoop/callback
WHOOP_CLIENT_ID=your_whoop_client_id
WHOOP_CLIENT_SECRET=your_whoop_client_secret
WHOOP_REDIRECT_URI=http://127.0.0.1:3001/auth/whoop/callback

# Server Configuration
PORT=3001